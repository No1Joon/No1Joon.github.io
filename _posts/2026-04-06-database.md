---
layout: post
title: "데이터베이스 — PostgreSQL 16 · asyncpg · Alembic 마이그레이션"
description: "비동기 ORM 패턴과 4개 테이블 스키마, Alembic 마이그레이션 전략"
date: 2026-04-06
category: Database
tags: [PostgreSQL, asyncpg, Alembic, SQLAlchemy]
---

## 기술 목록

| 기술 | 버전 | 용도 |
|------|------|------|
| PostgreSQL | 16 | 메인 RDBMS |
| asyncpg | 0.30.x | 비동기 드라이버 |
| Alembic | 1.14.x | 스키마 마이그레이션 |

---

## 스키마 — 4개 테이블

```
scenarios          — GCP 프로젝트별 시뮬레이션 시나리오
scenario_steps     — 시나리오를 구성하는 단계별 패킷 흐름
gcp_connections    — 암호화된 Service Account 연결 정보
collected_events   — Live 모드에서 수집된 실시간 이벤트
```

### scenarios

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | SERIAL PK | — |
| name | VARCHAR | 시나리오 이름 |
| description | TEXT | 설명 |
| topology_json | JSONB | 노드/엣지 구성 |
| created_at | TIMESTAMPTZ | 생성 시각 |

### gcp_connections

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | SERIAL PK | — |
| project_id | VARCHAR | GCP 프로젝트 ID |
| encrypted_sa_json | BYTEA | Fernet 암호화된 SA JSON |
| display_name | VARCHAR | UI 표시용 이름 |

---

## asyncpg + SQLAlchemy async 연결

```python
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
from sqlalchemy.orm import sessionmaker

DATABASE_URL = "postgresql+asyncpg://user:pass@localhost/dbname"

engine = create_async_engine(DATABASE_URL, echo=False, pool_size=10)
AsyncSessionLocal = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

async def get_db():
    async with AsyncSessionLocal() as session:
        yield session
```

`expire_on_commit=False` — 커밋 후 속성 접근 시 추가 SELECT 방지. async 환경에서 lazy load가 불가능하기 때문에 필수 설정이다.

---

## Alembic 마이그레이션

```bash
# 새 마이그레이션 생성
alembic revision --autogenerate -m "add collected_events table"

# 최신 버전으로 마이그레이션
alembic upgrade head

# 롤백
alembic downgrade -1
```

`--autogenerate`는 SQLAlchemy 모델과 현재 DB 스키마를 비교해 diff를 자동 생성한다. 단, JSONB 인덱스나 커스텀 타입은 수동 검토가 필요하다.

---

## 연결 풀 설정

| 설정 | 값 | 이유 |
|------|-----|------|
| `pool_size` | 10 | 동시 GCP API 호출 수와 맞춤 |
| `max_overflow` | 5 | 트래픽 피크 대응 |
| `pool_timeout` | 30s | 연결 대기 상한 |
| `pool_recycle` | 3600s | 유휴 연결 회수 |
